# ⚠️ Пожалуйста, не закрывайте работу из-за времени последнего коммита

### Перед тем как делать выводы — **прочитайте пояснение ниже** ❤️

1) [В особенности эту часть..](#-немного-честности-и-пояснений)
---

# Pull Request Reviewer Service

Решение тестового задания для стажёра **Backend** (Авито).

Проект реализует всю основную логику, описанную в техническом задании и OpenAPI: команды, пользователи, создание Pull Request’ов, автоматическое назначение ревьюверов, reassign и merge. Все основные ручки сделаны, плюс есть несколько дополнительных, которые помогают в разработке и тестировании.

---

# 📑 Оглавление

1. [✨ Что реализовано](#-что-реализовано)
2. [📌 Важные замечания по API](#-важные-замечания-по-api)
3. [🐳 Запуск в Docker](#-запуск-в-docker)
4. [🏗️ Локальный запуск](#-локальный-запуск)
5. [🧪 Запуск тестов](#-запуск-тестов)
6. [🤝 Немного честности и пояснений](#-немного-честности-и-пояснений)


## ✨ Что реализовано

### Основные возможности

* Создание команды с участниками (`/team/add`)
* Получение команды по имени
* Создание Pull Request
* Автоматическое назначение **до двух активных** ревьюверов из команд автора (если входные данные позволяют)
* Reassign ревьювера (старый ревьювер должен быть назначен, PR не должен быть MERGED, новый выбирается случайно и только из активных участников команд старого ревьювера)
* Merge PR с идемпотентным поведением
* Просмотр PR, где пользователь является ревьювером
* Изменение активности пользователя (`is_active`)
* Генерация простой статистики

### Дополнительная логика

Помимо строгого следования ТЗ, я добавил несколько вещей, которые улучшили процесс разработки:

* **Отдельная ручка `/team/add_or_update`**
  В OpenAPI из задания ручка была описана как “создаёт или обновляет команду и пользователей”.
  Из-за спешки я понял это буквально и реализовал обновляющее поведение прямо в `/team/add`.
  Но позже понял, что по ТЗ `/team/add` должна работать строго как создание и при повторном вызове возвращать `400 TEAM_EXISTS`.

  Сейчас всё аккуратно разделено:

  * `/team/add` — строгое создание по ТЗ (второй вызов = `400 TEAM_EXISTS`).
  * `/team/add_or_update` — вспомогательная ручка, реализующая мою первоначальную “создай/обнови” логику.
    Она используется в тестах и иногда удобна в разработке, но в спецификацию задания не входит.

* **Полная интеграционная тестовая база**
  Покрывает команды, пользователей, PR-флоу, reassign, merge и статистику.
  Тесты генерируют много фиктивных данных — поэтому лучше **не запускать их на продовом окружении**.
  Впрочем, как сидер/фабрика для dev они работают неплохо, а в проде тесты запускать уже поздно.

* **Пользователь может быть в нескольких командах**
  OpenAPI это не запрещает, поэтому такая возможность оставлена.
  Если пользователь деактивирован (`is_active=False`), это влияет только на будущие операции — старые записи остаются как есть.

* **Swagger UI и openapi.json**
  После запуска проекта:

  ```
  http://localhost:8080/docs
  ```

  Открытый OpenAPI JSON:

  ```
  http://localhost:8080/openapi.json
  ```

---

## 📌 Важные замечания по API

### Префикс `/api/v1/`

В оригинальном OpenAPI из задания пути начинались просто с `/`, без префикса.
По привычке я оформил API с версированием (`/api/v1/...`).

Когда заметил несоответствие — тесты уже зависели от этого пути.
Префикс работает корректно, но **при желании можно отключить**:

В 👉 [`app/main.py`, строка 25](https://github.com/bezura/BackendTestTask/blob/main/app/main.py#L25)

```python
app.include_router(api_router, prefix=settings.API_V1_PATH)
```

Можно закомментировать `prefix=settings.API_V1_PATH`.

---

## 🐳 Запуск в Docker

```bash
docker-compose up --build
```

API поднимется на:

```
http://localhost:8080/api/v1/
```

---

## 🏗️ Локальный запуск

Для локальной разработки нужно, чтобы **PostgreSQL был запущен заранее**, а `.env` настроен. Поэтому советую Docker

1. Создать или экспортировать переменные окружения
2. Установить зависимости:

   ```bash
   uv sync
   ```
3. Применить миграции:

   ```bash
   uv run alembic upgrade head
   ```
4. Запустить API:

   ```bash
   uv run uvicorn app.main:app --reload --port 8080
   ```

---

## 🧪 Запуск тестов

Тесты ничего дополнительно не требуют — они сами создадут тестовую схему и наполнят её фиктивными данными.

**Важно:**
Запускать тесты нужно **не в продовом окружении**,
потому что данных создаётся много (по крайней мере должно быть именно так) — но это, кстати, иногда удобно при разработке.

```bash
export $(cat .env | xargs)
uv run pytest -q tests
```

Тесты работают в отдельной схеме (`TEST_SCHEMA`) — боевые данные трогать не должны (но это не точно 🙃).
В любом случае использовать лучше только в dev.

---

## 🤝 Немного честности и пояснений

И последнее, но важное.

**Письмо от Авито с заданием попало в спам**, и я узнал о нём очень поздно.
Фактически я начал писать проект примерно за **30–40 минут до первого коммита**, причём почти сразу начались перебои с интернетом а потом он и вовсе пропал.

Работал в очень сжатых условиях, но в итоге смог довести проект до приемлемого состояния:

* сделал всю основную логику,
* добавил полноценные тесты,
* оставил удобные вспомогательные инструменты,
* оформил API максимально аккуратно.

Буду рад, если мою работу посмотрят и оценят.

Заранее спасибо ❤️